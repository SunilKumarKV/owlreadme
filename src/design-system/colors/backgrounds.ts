import { BackgroundTokens } from '../types/colors';
import { palette } from './palette';

export const backgrounds: BackgroundTokens = {
  primary: palette.navy.base,
  secondary: palette.navy.dark,
  darkNavy: palette.navy.darker,
  surfaceGlass: 'rgba(11, 15, 23, 0.75)',
  overlay: 'rgba(3, 7, 18, 0.85)',
  card: 'rgba(17, 24, 39, 0.6)',
};
