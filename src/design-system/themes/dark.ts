import { ThemeTokens } from '../types/colors';
import { semanticColors } from '../colors/semantic-colors';
import { gradients } from '../colors/gradients';
import { borders } from '../colors/borders';
import { backgrounds } from '../colors/backgrounds';

export const darkTheme: ThemeTokens = {
  mode: 'dark',
  semantic: semanticColors,
  gradients,
  borders,
  backgrounds,
};
